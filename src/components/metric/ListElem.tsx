import React, { Component } from 'react';

type Iprops = {

}

export default (props: Iprops) => {
    return (
        <React.Fragment>
            <span>Metric list elem component</span>
        </React.Fragment>
    )
}